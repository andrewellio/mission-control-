const weather = require("./Weather");
const { default: WeatherProvider } = require("./Weather");

//test("Testing OpenWeather URL returns something", () => {
   // const API = `https://api.openweathermap.org/data/2.5/weather?lat=-41&lon=174&APPID=9dbce15b12f75333f8f274f51793d0ff&mode=json&units=metric`;
   // var response = fetch(API);
  
//});

