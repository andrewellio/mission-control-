import React from "react";

/**
 * This is the loading screen component that is displayed.
 * It is likely to be used across multiple components.
 * We define it once here, so it can be refactored easily.
 */
const Loading = () => {
  return <h1>Loading...</h1>;
};

export default Loading;
